import { NavBar } from "./NavBar"
import { Banner } from "./Banner"

export const Header = () => {
  return (
    <section className="relative">
        <NavBar />
        <Banner />
    </section>
  )
}
